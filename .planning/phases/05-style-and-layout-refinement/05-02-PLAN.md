---
phase: 05-style-and-layout-refinement
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - styles.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "Body background is flat #111210, no radial gradient overlays"
    - "Cards (skills, experience) have background #181815 and border 0.5px solid #2a2820 and border-radius 10px"
    - "Hero name: Erick is Fraunces 600 normal #f0ebe3, Faria is Fraunces 300 italic #7a7468"
    - "Primary CTA is a white pill (#ffffff background, #111210 text)"
    - "Metrics widget is a dark card with amber Fraunces numbers and mono descriptions"
    - "Timeline renders as 2-column grid: 70px year column + content column"
    - "Section labels show // prefix in amber mono uppercase"
    - "No hover states, no transition effects, no animations"
    - "Page layout is responsive: collapses to 1-col at 920px"
  artifacts:
    - path: "styles.css"
      provides: "Complete visual token system and component styles"
      contains: "--bg: #111210"
    - path: "styles.css"
      provides: "Timeline 2-col grid"
      contains: "grid-template-columns: 70px 1fr"
    - path: "styles.css"
      provides: "White pill CTA"
      contains: "background: #ffffff"
  key_links:
    - from: "styles.css (.topbar)"
      to: "index.html nav.topbar"
      via: "class selector"
      pattern: "^\\.topbar"
    - from: "styles.css ([data-metrics-list])"
      to: "index.html div.metrics-widget[data-metrics-list]"
      via: "attribute selector"
      pattern: "\\[data-metrics-list\\]"
    - from: "styles.css (.timeline-item)"
      to: "renderExperience() generated markup"
      via: "JS-generated class, cannot change"
      pattern: "\\.timeline-item"
---

<objective>
Completely rewrite styles.css with the new design system: flat dark tokens, all component styles for the restructured markup from Plan 01. Every visual decision (D-01 through D-25) is implemented here.

Purpose: Plan 01 created the markup skeleton with new class names. This plan applies all visual styling. Together they produce the finished redesign.
Output: A complete styles.css targeting the new class structure. No old warm-amber gradient styles survive.
</objective>

<execution_context>
@Z:/Documents/projetos/pg-curriculo/.claude/get-shit-done/workflows/execute-plan.md
@Z:/Documents/projetos/pg-curriculo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-style-and-layout-refinement/05-CONTEXT.md
@.planning/phases/05-style-and-layout-refinement/05-RESEARCH.md
@.planning/phases/05-style-and-layout-refinement/05-01-SUMMARY.md

<interfaces>
<!-- Class names in new index.html markup (from Plan 01) — CSS must target these -->
<!-- JS-generated class names cannot change — CSS must match what renderX() outputs -->

New static markup classes:
  .page-shell          — max-width wrapper
  .topbar              — nav bar (MUST keep this class — installLanguageToggle targets it)
  .brand               — ~/erick-faria.dev brand link
  .brand-prefix        — the ~/ part (amber colored)
  .topbar-nav          — container for about/work/contact links
  .nav-link            — individual nav link
  .hero                — 2-col grid section
  .hero-copy           — left column
  .availability-pill   — amber pill badge [data-hero-availability]
  .name-first          — Erick span (Fraunces 600, primary color)
  .name-last           — Faria span (Fraunces 300 italic, muted)
  .hero-summary        — intro paragraph [data-hero-summary]
  .cta-group           — wraps CTA button + hint
  .primary-cta         — white pill download button [data-resume-download]
  .cta-hint            — "PDF · PT-BR & EN" text below button
  .metrics-widget      — right column, also has [data-metrics-list]
  .body-grid           — 2-col card grid
  .ph-card             — card container (skills + experience)
  .section-label       — // prefix label
  .ph-footer           — footer with contact links

JS-generated classes (renderX() output — DO NOT rename in CSS):
  .metric-card         — renderMetrics() article wrapper
  .metric-value        — renderMetrics() value paragraph
  .metric-label        — renderMetrics() label paragraph
  .skill-card          — renderSkills() article wrapper
  .skill-label         — renderSkills() group label
  .timeline-item       — renderExperience() article wrapper
  .timeline-meta       — renderExperience() period/location div
  .timeline-company    — renderExperience() company paragraph
  .contact-link        — renderContactLinks() anchor
  .language-toggle     — installLanguageToggle() div
  .language-button     — installLanguageToggle() button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CSS token system and base styles</name>
  <files>styles.css</files>

  <read_first>
    - styles.css (full file) — current content to fully replace
    - .planning/phases/05-style-and-layout-refinement/05-CONTEXT.md §Color Palette, §Typography decisions
    - .planning/phases/05-style-and-layout-refinement/05-RESEARCH.md §New CSS token system (full :root replacement)
  </read_first>

  <action>
COMPLETELY REPLACE styles.css. Do not preserve any existing rules. Write a fresh file starting with:

```css
/* ============================================================
   Phase 05 — Style & Layout Refinement
   Token system: flat dark / warm amber / editorial mono
   ============================================================ */

:root {
  --bg: #111210;
  --bg-card: #181815;
  --border: #2a2820;
  --border-width: 0.5px;
  --accent: #c9851e;
  --text-primary: #f0ebe3;
  --text-body: #e2ddd6;
  --text-muted: #7a7468;
  --text-very-muted: #4a4840;
  --text-ultra-muted: #3a3830;
  --radius-card: 10px;
  --radius-pill: 100px;
  --max-width: 1100px;
  --font-serif: "Fraunces", serif;
  --font-sans: "Plus Jakarta Sans", sans-serif;
  --font-mono: "JetBrains Mono", monospace;
}

/* Base reset */
*,
*::before,
*::after {
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  margin: 0;
  min-height: 100vh;
  font-family: var(--font-sans);
  font-weight: 400;
  background: var(--bg);
  color: var(--text-body);
  overflow-x: hidden;
}

a {
  color: inherit;
  text-decoration: none;
}

button {
  font: inherit;
  cursor: pointer;
}

h1, h2, h3, h4, h5, h6 {
  margin: 0;
}

p {
  margin: 0;
}
```

(This is Task 1 only — tokens + base reset. Tasks 2 and 3 append component and layout rules.)

Wait — this plan must be written as a single coordinated CSS file. Given the interdependency, write the ENTIRE styles.css in this one task as follows. The file sections are:

**Section 1: Tokens + Base** (above)

**Section 2: Page shell**
```css
/* ── Page shell ─────────────────────────────────────────── */
.page-shell {
  width: min(100%, var(--max-width));
  margin: 0 auto;
  padding: 2rem 1.5rem 4rem;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}
```

**Section 3: Topbar** (D-12)
```css
/* ── Topbar ──────────────────────────────────────────────── */
.topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  padding-bottom: 1.5rem;
  border-bottom: var(--border-width) solid var(--border);
}

.brand {
  font-family: var(--font-mono);
  font-size: 0.85rem;
  color: var(--text-muted);
  letter-spacing: -0.01em;
}

.brand-prefix {
  color: var(--accent);
}

.topbar-nav {
  display: flex;
  align-items: center;
  gap: 1.5rem;
}

.nav-link {
  font-family: var(--font-mono);
  font-size: 0.72rem;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: var(--text-muted);
}

/* Language toggle (appended by JS) */
.language-toggle {
  display: inline-flex;
  gap: 0.35rem;
  margin-left: auto;
}

.language-button {
  font-family: var(--font-mono);
  font-size: 0.72rem;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  padding: 0.35rem 0.65rem;
  border-radius: var(--radius-pill);
  border: var(--border-width) solid var(--border);
  background: transparent;
  color: var(--text-muted);
}

.language-button[data-active="true"] {
  border-color: var(--accent);
  color: var(--accent);
  background: rgba(201, 133, 30, 0.08);
}
```

**Section 4: Hero** (D-13, D-10, D-19, D-06)
```css
/* ── Hero ────────────────────────────────────────────────── */
.hero {
  display: grid;
  grid-template-columns: 1fr 300px;
  gap: 2rem;
  align-items: start;
}

.hero-copy {
  display: flex;
  flex-direction: column;
  gap: 1.25rem;
  min-width: 0;
}

/* Availability pill (D-19) */
.availability-pill {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  padding: 0.35rem 0.75rem;
  border: var(--border-width) solid var(--accent);
  border-radius: var(--radius-pill);
  background: rgba(201, 133, 30, 0.08);
  font-family: var(--font-mono);
  font-size: 0.72rem;
  color: var(--accent);
  width: fit-content;
}

.availability-pill::before {
  content: "●";
  font-size: 0.5rem;
}

/* Hero name split (D-10) */
h1 {
  font-size: clamp(2.75rem, 6vw, 5rem);
  line-height: 1;
  overflow-wrap: anywhere;
}

.name-first {
  font-family: var(--font-serif);
  font-weight: 600;
  font-style: normal;
  color: var(--text-primary);
}

.name-last {
  font-family: var(--font-serif);
  font-weight: 300;
  font-style: italic;
  color: var(--text-muted);
}

/* Hero intro paragraph */
.hero-summary {
  font-size: 1rem;
  font-weight: 300;
  line-height: 1.7;
  color: var(--text-body);
  max-width: 52ch;
}

/* CTA group */
.cta-group {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 0.6rem;
}

/* Primary CTA — white pill (D-06) */
.primary-cta {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 0.75rem 1.75rem;
  border-radius: var(--radius-pill);
  background: #ffffff;
  color: #111210;
  font-family: var(--font-sans);
  font-weight: 600;
  font-size: 0.9rem;
  border: none;
  min-height: 2.75rem;
}

/* CTA hint text (D-11) */
.cta-hint {
  font-family: var(--font-mono);
  font-size: 0.68rem;
  color: var(--text-very-muted);
  letter-spacing: 0.04em;
}
```

**Section 5: Metrics widget** (D-14)
```css
/* ── Metrics widget ──────────────────────────────────────── */
/* Container doubles as the data-metrics-list target */
.metrics-widget,
[data-metrics-list] {
  background: var(--bg-card);
  border: var(--border-width) solid var(--border);
  border-radius: var(--radius-card);
  padding: 1.25rem;
  display: flex;
  flex-direction: column;
  min-width: 0;
}

.metric-card {
  padding: 0.9rem 0;
  border-bottom: var(--border-width) solid var(--border);
}

.metric-card:first-child {
  padding-top: 0;
}

.metric-card:last-child {
  border-bottom: none;
  padding-bottom: 0;
}

.metric-value {
  font-family: var(--font-serif);
  font-weight: 600;
  font-size: 1.5rem;
  color: var(--accent);
  line-height: 1;
}

.metric-label {
  font-family: var(--font-mono);
  font-size: 0.68rem;
  color: var(--text-muted);
  margin-top: 0.3rem;
  letter-spacing: 0.02em;
}
```

**Section 6: Body grid and cards** (D-15, D-18, D-22)
```css
/* ── Body grid ───────────────────────────────────────────── */
.body-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1.25rem;
  align-items: start;
}

/* Shared card style (D-22) */
.ph-card {
  background: var(--bg-card);
  border: var(--border-width) solid var(--border);
  border-radius: var(--radius-card);
  padding: 1.5rem;
  min-width: 0;
}

/* Section label with // prefix (D-18) */
.section-label {
  font-family: var(--font-mono);
  font-size: 0.68rem;
  text-transform: uppercase;
  letter-spacing: 0.12em;
  color: var(--accent);
  margin-bottom: 1.25rem;
}

.section-label::before {
  content: "// ";
}
```

**Section 7: Skills** (D-20 — uniform amber pill, gap documented)
```css
/* ── Skills ──────────────────────────────────────────────── */
/* D-20 gap: two-tier (core/secondary) requires tier field in config.js data contract.
   Since config.js cannot change in this phase, all skill tags use the amber-tinted
   pill (core style) uniformly. Tier differentiation is deferred. */

.skill-card {
  margin-bottom: 1.25rem;
}

.skill-card:last-child {
  margin-bottom: 0;
}

.skill-label {
  font-family: var(--font-mono);
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--text-muted);
  margin-bottom: 0.65rem;
}

.skill-card ul {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-wrap: wrap;
  gap: 0.45rem;
}

.skill-card ul li {
  font-family: var(--font-mono);
  font-size: 0.72rem;
  padding: 0.3rem 0.65rem;
  border-radius: var(--radius-pill);
  background: rgba(201, 133, 30, 0.12);
  color: var(--accent);
  border: var(--border-width) solid var(--accent);
  overflow-wrap: anywhere;
}
```

**Section 8: Experience timeline** (D-21)
```css
/* ── Experience timeline ─────────────────────────────────── */
/* D-21: CSS-only 2-col grid on JS-generated .timeline-item markup.
   The renderer outputs: article.timeline-item > div.timeline-meta + h3 + p.timeline-company + p
   We cannot add a wrapper div for the content column, so we use grid-column assignment. */

[data-experience-list] {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

.timeline-item {
  display: grid;
  grid-template-columns: 70px 1fr;
  gap: 0.2rem 1rem;
  align-items: start;
}

/* Year column */
.timeline-meta {
  grid-column: 1;
  grid-row: 1 / -1;
  font-family: var(--font-mono);
  font-size: 0.65rem;
  color: var(--text-ultra-muted);
  line-height: 1.5;
  padding-top: 0.15rem;
}

/* Hide the location span inside timeline-meta — period (year) is enough */
.timeline-meta span:last-child {
  display: none;
}

/* Content column items */
.timeline-item h3 {
  grid-column: 2;
  font-family: var(--font-sans);
  font-weight: 500;
  font-size: 0.95rem;
  color: var(--text-primary);
  line-height: 1.3;
}

.timeline-company {
  grid-column: 2;
  font-family: var(--font-mono);
  font-size: 0.68rem;
  color: var(--text-muted);
  letter-spacing: 0.02em;
}

.timeline-item p:last-of-type {
  grid-column: 2;
  font-size: 0.875rem;
  color: var(--text-body);
  line-height: 1.65;
}
```

**Section 9: Footer** (D-16)
```css
/* ── Footer ──────────────────────────────────────────────── */
.ph-footer {
  border-top: var(--border-width) solid var(--border);
  padding-top: 1.25rem;
}

[data-contact-links] {
  display: flex;
  flex-wrap: wrap;
  gap: 1.5rem;
  align-items: center;
}

.contact-link {
  font-family: var(--font-mono);
  font-size: 0.72rem;
  color: var(--text-muted);
  display: flex;
  flex-direction: column;
  gap: 0.2rem;
  min-width: 0;
}

.contact-link span {
  font-size: 0.62rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--text-very-muted);
}

.contact-link strong {
  color: var(--text-body);
  font-weight: 400;
  overflow-wrap: anywhere;
}
```

**Section 10: Responsive breakpoints**
```css
/* ── Responsive ──────────────────────────────────────────── */

/* Tablet: collapse 2-col grids to 1-col */
@media (max-width: 920px) {
  .hero {
    grid-template-columns: 1fr;
  }

  /* Move metrics widget below hero copy */
  .metrics-widget,
  [data-metrics-list] {
    order: 2;
  }

  .body-grid {
    grid-template-columns: 1fr;
  }

  .topbar-nav {
    display: none;
  }
}

/* Mobile: timeline inner grid collapses (Pitfall 4 from RESEARCH.md) */
@media (max-width: 640px) {
  .timeline-item {
    grid-template-columns: 1fr;
  }

  .timeline-meta {
    grid-row: auto;
    margin-bottom: 0.35rem;
  }

  .timeline-item h3,
  .timeline-company,
  .timeline-item p:last-of-type {
    grid-column: 1;
  }

  .page-shell {
    padding-inline: 0.75rem;
  }

  [data-contact-links] {
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
  }
}

/* Small phone */
@media (max-width: 480px) {
  .page-shell {
    padding-inline: 0.55rem;
    padding-bottom: 3rem;
  }

  h1 {
    font-size: clamp(2.25rem, 10vw, 3rem);
  }

  .hero-summary {
    max-width: 100%;
  }

  .language-toggle {
    flex-wrap: wrap;
  }

  .language-button {
    flex: 1 1 auto;
  }
}
```

Write all 10 sections as one complete file. Do not omit any section.

Note on D-23 (no hover states): Do NOT add any `:hover`, `:focus-visible`, or `transition` rules to any element. The existing `.language-button` focus behavior from the JS (data-active) is sufficient.
  </action>

  <verify>
    <automated>node -e "
const fs = require('fs');
const css = fs.readFileSync('Z:/Documents/projetos/pg-curriculo/styles.css', 'utf8');
const checks = [
  ['--bg: #111210', css.includes('--bg: #111210')],
  ['--bg-card: #181815', css.includes('--bg-card: #181815')],
  ['--accent: #c9851e', css.includes('--accent: #c9851e')],
  ['--border: #2a2820', css.includes('--border: #2a2820')],
  ['--radius-card: 10px', css.includes('--radius-card: 10px')],
  ['no radial-gradient on body', !css.includes('radial-gradient(circle at top')],
  ['no hover CTA', !css.includes('.primary-cta:hover')],
  ['white pill CTA', css.includes('background: #ffffff')],
  ['name-first Fraunces 600', css.includes('.name-first') && css.includes('font-weight: 600')],
  ['name-last italic 300', css.includes('.name-last') && css.includes('font-weight: 300') && css.includes('font-style: italic')],
  ['name-last muted color', css.includes('color: var(--text-muted)') || css.includes('color: #7a7468')],
  ['timeline 70px col', css.includes('grid-template-columns: 70px 1fr')],
  ['section-label before', css.includes('content: \"// \"') || css.includes(\"content: '// '\")],
  ['metrics-widget flex-col', css.includes('flex-direction: column')],
  ['border 0.5px', css.includes('0.5px')],
  ['border-radius 10px card', css.includes('--radius-card: 10px')],
  ['920px breakpoint', css.includes('max-width: 920px')],
  ['640px breakpoint', css.includes('max-width: 640px')],
];
checks.forEach(([name, ok]) => console.log((ok ? 'PASS' : 'FAIL') + ' ' + name));
const failed = checks.filter(([,ok]) => !ok);
if (failed.length) process.exit(1);
"
</automated>
  </verify>

  <acceptance_criteria>
    - All 18 checks above print PASS (zero FAIL lines)
    - `grep "radial-gradient" styles.css` returns no results (D-05: no gradient overlays)
    - `grep "hover" styles.css` returns no results (D-23: no hover states)
    - `grep "transition" styles.css` returns no results (D-24: no animations)
    - `grep "grid-template-columns: 70px 1fr" styles.css` returns a match (D-21)
    - `grep "#ffffff" styles.css` returns a match (D-06: white CTA)
    - `grep "font-style: italic" styles.css` returns a match (D-10)
    - `grep "content: \"// \"" styles.css` OR `grep "content: '// '" styles.css` returns a match (D-18)
    - `grep "#111210" styles.css` returns at least 2 matches (body bg token + CTA text color)
    - File length is at least 300 lines (complete rewrite, not partial)
  </acceptance_criteria>

  <done>styles.css fully rewritten with flat dark token system, all component styles for new markup, no hover states, no gradients, no transitions. Timeline 2-col grid, white pill CTA, amber metrics, section // labels, and responsive breakpoints all in place.</done>
</task>

</tasks>

<verification>
Open index.html in browser after both Plan 01 and Plan 02 are complete:

1. Background is flat near-black (#111210) — no radial gradient glow
2. Hero shows "Erick" in heavy upright serif and "Faria" in lighter italic muted serif on same line
3. Availability badge appears as amber-outlined pill with amber dot prefix
4. CTA button is white pill with dark text
5. "PDF · PT-BR & EN" hint text appears below CTA in small mono
6. Right side of hero shows metrics numbers in amber Fraunces serif
7. Body grid shows two cards: skills on left, experience timeline on right
8. Skills show as amber-tinted pills
9. Experience timeline: year in ultra-muted mono, role + company + summary in content column
10. Footer shows contact links in mono, separated by hairline top border
11. No section appears that uses numbered 01/02/03 headings or the old amber gradient header
12. Language toggle appears in topbar; clicking EN/PT-BR switches content
13. Resize to 920px — hero and body-grid collapse to 1 column
14. Resize to 640px — timeline year stacks above role (no 2-col)
</verification>

<success_criteria>
- styles.css is a complete rewrite with zero preserved old rules
- All D-01 through D-25 decisions implemented (D-20 gap documented in comment)
- No hover states, no transitions, no radial-gradient body backgrounds
- Token names: --bg, --bg-card, --border, --accent, --text-primary, --text-body, --text-muted, --text-very-muted, --text-ultra-muted, --radius-card, --radius-pill
- D-06: .primary-cta background: #ffffff, color: #111210
- D-10: .name-first Fraunces 600 normal, .name-last Fraunces 300 italic #7a7468
- D-21: .timeline-item grid-template-columns: 70px 1fr
- D-18: .section-label::before content: "// "
- D-22: --radius-card: 10px
- Responsive: 920px collapses hero + body-grid, 640px collapses timeline inner grid
</success_criteria>

<output>
After completion, create `.planning/phases/05-style-and-layout-refinement/05-02-SUMMARY.md` following the summary template at @.claude/get-shit-done/templates/summary.md
</output>
