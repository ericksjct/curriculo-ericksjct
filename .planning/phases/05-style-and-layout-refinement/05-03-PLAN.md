---
phase: 05-style-and-layout-refinement
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "Recruiter arriving at the page gets a coherent visual impression matching the mockup direction"
    - "Language toggle works and switches all content correctly"
    - "Resume download link triggers a file download"
    - "Page is usable on mobile (no overflow, no broken layout)"
    - "No JavaScript errors in browser console"
  artifacts: []
  key_links: []
---

<objective>
Human visual verification that the redesigned page matches the locked design decisions and functions correctly end-to-end. This is the acceptance gate for Phase 05.

Purpose: Visual design correctness cannot be confirmed by grep or CLI commands. A human must open the page and confirm the amber editorial aesthetic, layout structure, and interactive behavior all match the intended direction.
Output: Confirmed approval (or a list of specific issues to address in a follow-up fix).
</objective>

<execution_context>
@Z:/Documents/projetos/pg-curriculo/.claude/get-shit-done/workflows/execute-plan.md
@Z:/Documents/projetos/pg-curriculo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-style-and-layout-refinement/05-CONTEXT.md
@.planning/phases/05-style-and-layout-refinement/05-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Full visual redesign of index.html + styles.css:
- New flat dark (#111210) background — no radial gradient glow
- Topbar with ~/erick-faria.dev brand and mono nav links
- Hero: 2-column grid with editorial name split (Erick bold / Faria italic muted) + amber availability pill + white pill CTA + metrics widget sidebar
- Body: 2-column card grid (skills pills left / experience timeline right)
- Footer: contact links in mono with hairline top border
- Full responsive collapse at 920px
- Language toggle (PT-BR / EN) preserved in topbar
  </what-built>

  <how-to-verify>
Open `index.html` directly in a browser (no server needed — static file).

**Step 1 — Console check**
Open DevTools > Console. Confirm: zero errors on load. Any "Cannot set properties of null" error means a required data-* element was accidentally removed.

**Step 2 — Visual check (desktop, ~1200px)**
Confirm each of these matches the design intent:
- [ ] Background is flat near-black — no amber glow or radial gradients
- [ ] Topbar shows `~/erick-faria.dev` with amber `~/` prefix, then mono nav links, then language buttons
- [ ] Hero left column: amber availability pill, then "Erick" (heavy upright) and "Faria" (light italic, muted gray) as one heading, then intro paragraph, then white pill button, then small mono hint "PDF · PT-BR & EN"
- [ ] Hero right column: dark card showing numeric metrics in amber serif with mono labels, hairline dividers between metrics
- [ ] Body: two side-by-side cards — left shows "// stack" label with amber-tinted skill pills; right shows "// experiência" label with timeline (year column + role/company/summary)
- [ ] Footer: hairline top border, contact links in mono (label small + value)
- [ ] No profile photo, no numbered section headings (01, 02...), no amber gradient header card

**Step 3 — Language toggle**
- Click "EN" — all copy switches to English, availability text, metrics labels, experience content
- Click "PT-BR" — all copy switches back to Portuguese
- Both toggle buttons reflect active state (amber border/color on active)

**Step 4 — Resume download**
- Click the white pill CTA — browser initiates a file download (or opens PDF in new tab)
- Confirm it does not navigate to a 404

**Step 5 — Mobile (resize to ~375px)**
- [ ] Hero stacks to 1 column (metrics below hero copy)
- [ ] Body cards stack vertically (skills above experience)
- [ ] Timeline year stacks above role (not side-by-side)
- [ ] No horizontal overflow (no horizontal scrollbar)
- [ ] Text remains readable, no content clipped
  </how-to-verify>

  <resume-signal>
Type "approved" if all checks pass.

If issues found, describe them specifically (e.g., "availability pill has no border", "metrics font is sans-serif not serif", "timeline year overlaps role text on mobile") so targeted fixes can be made.
  </resume-signal>
</task>

</tasks>

<verification>
Human approval is the verification for this plan. No automated checks apply.
</verification>

<success_criteria>
- User types "approved" after visual inspection
- Zero console errors confirmed
- Language toggle switches all content correctly
- Resume download works
- Mobile layout collapses correctly without overflow
</success_criteria>

<output>
After approval, create `.planning/phases/05-style-and-layout-refinement/05-03-SUMMARY.md` following the summary template at @.claude/get-shit-done/templates/summary.md

Also update `.planning/STATE.md`:
- status: complete (or awaiting next phase)
- last_activity: 2026-04-03 -- Phase 05 visual redesign complete
- stopped_at: Phase 05 complete
</output>
