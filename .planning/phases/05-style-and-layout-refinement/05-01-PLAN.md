---
phase: 05-style-and-layout-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "index.html compiles without any removed data-* element that lacks a null guard"
    - "The page renders content from config.js (language toggle functions, no TypeError in console)"
    - "Hero name displays as two visually distinct spans — Erick and Faria"
    - "Topbar contains .topbar class element so the language toggle installs correctly"
    - "Layout groups match new structure: topbar / hero 2-col / body-grid 2-col / footer"
  artifacts:
    - path: "index.html"
      provides: "New page layout structure"
      contains: "class=\"topbar\""
    - path: "index.html"
      provides: "Static hero name split"
      contains: "class=\"name-first\""
    - path: "index.html"
      provides: "Hidden sink elements for renderer safety"
      contains: "data-hero-highlights"
    - path: "index.html"
      provides: "Updated font URL with italic axis"
      contains: "Fraunces:ital,opsz,wght"
  key_links:
    - from: "index.html (.topbar)"
      to: "installLanguageToggle() in inline script"
      via: "query('.topbar').appendChild(toggle)"
      pattern: "class=\"topbar\""
    - from: "index.html ([data-hero-highlights])"
      to: "renderHighlights() in inline script"
      via: "direct innerHTML, no null guard"
      pattern: "data-hero-highlights"
    - from: "index.html ([data-hero-tags])"
      to: "renderHeroTags() in inline script"
      via: "direct innerHTML, no null guard"
      pattern: "data-hero-tags"
    - from: "index.html ([data-resume-download])"
      to: "downloadLink.href assignment in inline script"
      via: "direct property, no null guard"
      pattern: "data-resume-download"
---

<objective>
Restructure index.html from the current section-scroll layout to the new 2-column composition (topbar / hero grid / body-grid / footer). Update the Google Fonts URL for new weight axes. Insert hidden sink elements for renderer safety. Produce the static hero name split markup.

Purpose: The CSS rewrite in Plan 02 targets classes defined in this new markup. The markup restructure must happen first so Plan 02 has real class names to style.
Output: A fully restructured index.html that renders content from config.js without any TypeError, with the new layout skeleton in place (unstyled until Plan 02 applies CSS).
</objective>

<execution_context>
@Z:/Documents/projetos/pg-curriculo/.claude/get-shit-done/workflows/execute-plan.md
@Z:/Documents/projetos/pg-curriculo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-style-and-layout-refinement/05-CONTEXT.md
@.planning/phases/05-style-and-layout-refinement/05-RESEARCH.md

<interfaces>
<!-- Inline script selectors with NO null guard — these elements MUST exist in the new DOM -->
<!-- Source: index.html lines 134-265, verified by 05-RESEARCH.md §Critical: data-* Survival Map -->

MUST RETAIN (no null guard — TypeError if absent):
  [data-hero-highlights]   → renderHighlights() — line 135: root.innerHTML = ...
  [data-hero-tags]         → renderHeroTags()   — line 150: root.innerHTML = ...
  [data-skills-list]       → renderSkills()     — line 154: root.innerHTML = ...
  [data-experience-list]   → renderExperience() — line 170: root.innerHTML = ...
  [data-metrics-list]      → renderMetrics()    — line 189: root.innerHTML = ...
  [data-contact-links]     → renderContactLinks()— line 203: root.innerHTML = ...
  [data-resume-download]   → downloadLink.href  — line 257: downloadLink.href = ...
  [data-meta-description]  → setAttribute()     — line 228: .setAttribute("content", ...)
  .topbar                  → installLanguageToggle() — line 292: query(".topbar").appendChild(toggle)

SAFE TO REMOVE (applyText has null guard — if (node) check):
  [data-site-kicker], [data-site-location], [data-contact-shortcut]
  [data-hero-headline]  ← IMPORTANT: remove from h1, but keep as hidden span for document.title
  [data-hero-card-label], [data-hero-card-title], [data-hero-card-body]
  [data-about-kicker], [data-about-title], [data-about-body]
  [data-skills-kicker], [data-skills-title]
  [data-experience-kicker], [data-experience-title]
  [data-metrics-kicker], [data-metrics-title]
  [data-contact-kicker], [data-contact-title], [data-contact-body]
  [data-contact-cta], [data-hero-availability] (keep — pill badge)

Renderer-generated class names (CSS in Plan 02 must target these):
  .timeline-item, .timeline-meta, .timeline-company  — from renderExperience()
  .metric-card, .metric-value, .metric-label         — from renderMetrics()
  .skill-card, .skill-label                          — from renderSkills()
  .contact-link                                      — from renderContactLinks()
  .language-toggle, .language-button                 — from installLanguageToggle()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update font URL and head metadata</name>
  <files>index.html</files>

  <read_first>
    - index.html (lines 1-20) — current font link tag to replace, meta elements to preserve
    - .planning/phases/05-style-and-layout-refinement/05-RESEARCH.md §Font URL Change — exact replacement URL
  </read_first>

  <action>
In index.html, replace the single Google Fonts `<link href="https://fonts.googleapis.com/css2?...">` tag (currently line 15) with this exact URL:

```html
<link
  href="https://fonts.googleapis.com/css2?family=Fraunces:ital,opsz,wght@0,9..144,600;1,9..144,300;1,9..144,500&family=JetBrains+Mono:wght@400;500&family=Plus+Jakarta+Sans:wght@300;400;500;600&display=swap"
  rel="stylesheet"
/>
```

Keep all other `<head>` content unchanged:
- `<meta charset="UTF-8" />`
- `<meta name="viewport" ...>`
- `<title>Curriculo Erick</title>`
- `<meta name="description" ... data-meta-description />` — MUST stay, no null guard
- Both `<link rel="preconnect" ...>` tags
- `<link rel="stylesheet" href="styles.css" />`
- `<script src="config.js"></script>`

Do NOT add, remove, or reorder any other head element.
  </action>

  <verify>
    <automated>grep -n "Fraunces:ital,opsz,wght@0,9..144,600;1,9..144,300;1,9..144,500" "Z:/Documents/projetos/pg-curriculo/index.html"</automated>
  </verify>

  <acceptance_criteria>
    - grep finds `Fraunces:ital,opsz,wght@0,9..144,600;1,9..144,300;1,9..144,500` in index.html
    - grep finds `JetBrains+Mono:wght@400;500` in index.html
    - grep finds `Plus+Jakarta+Sans:wght@300;400;500;600` in index.html
    - grep finds `data-meta-description` in index.html (not removed)
    - grep finds `config.js` script tag still present in head
  </acceptance_criteria>

  <done>Font link loads italic Fraunces 300/500, regular Fraunces 600, JetBrains Mono 400/500, Plus Jakarta Sans 300–600. Head metadata elements unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure body markup to new layout</name>
  <files>index.html</files>

  <read_first>
    - index.html (full file, lines 21-307) — current body markup to replace
    - .planning/phases/05-style-and-layout-refinement/05-RESEARCH.md §New Layout Structure — target layout skeleton
    - .planning/phases/05-style-and-layout-refinement/05-RESEARCH.md §Hero Highlights vs Metrics Widget — hidden sink pattern
    - .planning/phases/05-style-and-layout-refinement/05-RESEARCH.md §Hero Name Split (D-10)
    - .planning/phases/05-style-and-layout-refinement/05-CONTEXT.md §decisions D-10 through D-19
  </read_first>

  <action>
Replace the entire `<body>` content (keep the inline `<script>` block unchanged — every line from `(function renderPortfolio()` to the closing `</script>`) with this new structure:

```html
<body>
  <div class="page-shell">

    <nav class="topbar">
      <a class="brand" href="#hero">
        <span class="brand-prefix">~/</span>erick-faria.dev
      </a>
      <div class="topbar-nav">
        <a class="nav-link" href="#hero">about</a>
        <a class="nav-link" href="#experience">work</a>
        <a class="nav-link" href="#footer">contact</a>
      </div>
      <!-- language toggle appended here by installLanguageToggle() — .topbar class required -->
    </nav>

    <section class="hero" id="hero">
      <div class="hero-copy">
        <!-- Availability pill — applyText guards with if(node), safe to retain -->
        <p class="availability-pill" data-hero-availability></p>

        <!-- Hidden sink: renderer writes document.title via data-hero-headline textContent -->
        <span data-hero-headline style="display:none"></span>
        <!-- Static name split — italic/weight split not achievable via textContent (D-10) -->
        <h1>
          <span class="name-first">Erick</span>
          <span class="name-last">Faria</span>
        </h1>

        <p class="hero-summary" data-hero-summary></p>

        <div class="cta-group">
          <a class="primary-cta" data-resume-download target="_blank" rel="noreferrer">
            <span data-cta-primary></span>
          </a>
          <p class="cta-hint">PDF · PT-BR &amp; EN</p>
        </div>

        <!-- Hidden sinks: renderHighlights and renderHeroTags have no null guard -->
        <dl style="display:none" data-hero-highlights></dl>
        <div style="display:none" data-hero-tags></div>
      </div>

      <!-- Metrics widget: right column of hero 2-col grid (D-13/D-14) -->
      <!-- renderMetrics() writes .innerHTML here — no null guard, must exist -->
      <div class="metrics-widget" data-metrics-list></div>
    </section>

    <div class="body-grid">
      <div class="ph-card" id="skills">
        <p class="section-label">stack</p>
        <!-- renderSkills() writes .innerHTML here — no null guard, must exist -->
        <div data-skills-list></div>
      </div>

      <div class="ph-card" id="experience">
        <p class="section-label">experiência</p>
        <!-- renderExperience() writes .innerHTML here — no null guard, must exist -->
        <div data-experience-list></div>
      </div>
    </div>

    <footer class="ph-footer" id="footer">
      <!-- renderContactLinks() writes .innerHTML here — no null guard, must exist -->
      <div data-contact-links></div>
    </footer>

  </div>

  <script>
    <!-- PASTE INLINE SCRIPT UNCHANGED — do NOT modify any JS -->
  </script>
</body>
```

CRITICAL rules for the inline `<script>`:
- Copy the entire existing script block from the current index.html verbatim (lines 121-305)
- Do NOT modify a single character of the JS
- The script must still end with `installLanguageToggle(); renderLanguage();`

Elements that MUST NOT be omitted (no null guard in renderer):
1. `data-hero-highlights` — `<dl style="display:none" data-hero-highlights></dl>`
2. `data-hero-tags` — `<div style="display:none" data-hero-tags></div>`
3. `data-skills-list` — inside `.ph-card#skills`
4. `data-experience-list` — inside `.ph-card#experience`
5. `data-metrics-list` — the `.metrics-widget` div
6. `data-contact-links` — inside `.ph-footer`
7. `data-resume-download` — on the `<a class="primary-cta">` anchor
8. `data-meta-description` — already in `<head>` (Task 1 preserved it)
9. `.topbar` — the `<nav class="topbar">` element

Removed elements (safe — applyText guards with `if (node)`):
- `.hero-backdrop` div — no longer needed (D-05: flat background)
- `.hero-card` aside with hero-card-label/title/body — removed in new design (D-17)
- All five `.content-section` sections (about, skills, experience, metrics, contact) — collapsed (D-17)
- `.secondary-cta` link with `data-contact-cta` — removed from hero (D-13)
- `.hero-proof` / proof highlights — replaced by hidden sink (D-13/D-14)
- `.brand-lockup`, `.eyebrow`, `data-site-kicker`, `data-site-location` — replaced by `.brand`
  </action>

  <verify>
    <automated>node -e "
const fs = require('fs');
const html = fs.readFileSync('Z:/Documents/projetos/pg-curriculo/index.html', 'utf8');
const checks = [
  ['data-hero-highlights', html.includes('data-hero-highlights')],
  ['data-hero-tags', html.includes('data-hero-tags')],
  ['data-skills-list', html.includes('data-skills-list')],
  ['data-experience-list', html.includes('data-experience-list')],
  ['data-metrics-list', html.includes('data-metrics-list')],
  ['data-contact-links', html.includes('data-contact-links')],
  ['data-resume-download', html.includes('data-resume-download')],
  ['class topbar', html.includes('class=\"topbar\"')],
  ['name-first span', html.includes('class=\"name-first\"')],
  ['name-last span', html.includes('class=\"name-last\"')],
  ['hero-highlights hidden', /data-hero-highlights[^>]*>/.test(html) && html.includes('display:none') || html.includes('style=\"display:none\"')],
  ['installLanguageToggle', html.includes('installLanguageToggle()')],
  ['renderLanguage', html.includes('renderLanguage()')],
];
checks.forEach(([name, ok]) => console.log((ok ? 'PASS' : 'FAIL') + ' ' + name));
const failed = checks.filter(([,ok]) => !ok);
if (failed.length) process.exit(1);
"
</automated>
  </verify>

  <acceptance_criteria>
    - All 13 checks above print PASS (zero FAIL lines)
    - `grep -c "data-hero-highlights" index.html` returns at least 1
    - `grep "data-hero-highlights" index.html` shows `style="display:none"` on the same line (hidden sink)
    - `grep "data-hero-tags" index.html` shows `style="display:none"` on the same line
    - `grep "name-first" index.html` shows `<span class="name-first">Erick</span>`
    - `grep "name-last" index.html` shows `<span class="name-last">Faria</span>`
    - `grep "class=\"topbar\"" index.html` finds the nav element
    - `grep "installLanguageToggle" index.html` finds the call (script intact)
    - No `.hero-backdrop` element remains in the body
    - No `.content-section` element remains in the body
    - No `.hero-card` aside element remains in the body
  </acceptance_criteria>

  <done>index.html body restructured to: topbar / hero (2-col: copy + metrics widget) / body-grid (2-col: skills + experience) / footer. All no-null-guard data-* elements present. Inline script unchanged. Hero name is two styled spans.</done>
</task>

</tasks>

<verification>
Open index.html in a browser. Expected state after Plan 01 (before Plan 02 CSS):
- Page renders without any JavaScript TypeError in the browser console
- Language toggle buttons appear in the topbar
- Clicking PT-BR / EN updates text content (proves renderer works)
- Hero shows "Erick Faria" as two separate inline elements
- Metrics numbers appear somewhere on the page (unstyled)
- Skills pills / experience timeline content renders (unstyled)
- Contact links render in the footer area (unstyled)

If console shows "Cannot set properties of null (setting 'innerHTML')" — a required data-* element is missing.
</verification>

<success_criteria>
- Google Fonts URL loads Fraunces italic 300+500 and regular 600, Plus Jakarta Sans 300–600, JetBrains Mono 400+500
- New layout skeleton: topbar / hero / body-grid / footer
- Hero name: `<span class="name-first">Erick</span> <span class="name-last">Faria</span>`
- Hidden sinks in place: `data-hero-highlights`, `data-hero-tags` both have `style="display:none"`
- `data-hero-headline` exists as hidden `<span>` for document.title assignment
- All 9 no-null-guard elements present in DOM
- Inline script 100% unchanged — zero JS modifications
- No browser console errors on page load
</success_criteria>

<output>
After completion, create `.planning/phases/05-style-and-layout-refinement/05-01-SUMMARY.md` following the summary template at @.claude/get-shit-done/templates/summary.md
</output>
